spring:
  profiles.active: test
datasource:
  host:
  port:
  database: testdb
  username: sa
  password:
  jdbc-driver-class: org.h2.Driver
  url: jdbc:h2:mem:${database}
  timeZone: Asia/Tokyo
  runtime-configuration-file: reladomo/config/TestReladomoRuntimeConfiguration.xml